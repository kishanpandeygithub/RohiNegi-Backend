// this is  the data base is desind for the storing for the book in the dataase 
const mongoose = require("mongoose");
const Books = require("./models/book")
const url =
    async function main() {
        await mongoose.connect(url);
    }

module.exports = main;
